% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/stats.R
\name{robinson_foulds}
\alias{robinson_foulds}
\title{Compute the Robinson-Foulds distance between two trees.}
\usage{
robinson_foulds(tree1, tree2, normalize = FALSE)

robinson_foulds(tree1, tree2, normalize = FALSE)
}
\arguments{
\item{tree1}{First tree as phylo2vec vector (1D) or matrix (2D).
Only topology is used; branch lengths are ignored.}

\item{tree2}{Second tree as phylo2vec vector (1D) or matrix (2D).
Only topology is used; branch lengths are ignored.}

\item{normalize}{If TRUE, return normalized distance in range \verb{[0.0, 1.0]}.
Default is FALSE.}

\item{v1}{First tree as phylo2vec vector}

\item{v2}{Second tree as phylo2vec vector}
}
\value{
RF distance (numeric)

RF distance (numeric). Integer value if normalize=FALSE,
float in \verb{[0, 1]} otherwise.
}
\description{
RF distance counts the number of bipartitions (splits) that differ
between two tree topologies. Lower values indicate more similar trees.

RF distance counts the number of bipartitions (splits) that differ
between two tree topologies. Lower values indicate more similar trees.
}
\examples{
v1 <- sample_tree(10, topology_only = TRUE)
v2 <- sample_tree(10, topology_only = TRUE)
robinson_foulds(v1, v1) # Identical trees: 0
robinson_foulds(v1, v2) # Different trees: >= 0
robinson_foulds(v1, v2, normalize = TRUE) # Normalized: [0, 1]
}
